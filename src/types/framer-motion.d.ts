declare module "framer-motion" {
  // Minimal type shims so the app can compile; the real library should be
  // installed via `npm install framer-motion` in the project.
  export const motion: any
  export const AnimatePresence: any
}


